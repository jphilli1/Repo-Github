# Split-RAG Extension — API-Free Local RAG Pipeline
# ABSOLUTE CONSTRAINT: No torch, transformers, llama-index, neo4j, openai, google-genai

# Core parsing (primary engine)
docling>=2.0.0
docling-core>=2.0.0

# Fallback parsing
pdfplumber>=0.10.0

# In-memory graph database
networkx>=3.0

# Lexical retrieval (TF-IDF + cosine similarity)
scikit-learn>=1.3.0

# Fuzzy entity matching
rapidfuzz>=3.0.0

# Data validation (v2 strictly enforced)
pydantic>=2.0.0

# Data manipulation
pandas>=2.0.0
numpy>=1.24.0

# Utilities
tqdm>=4.65.0
